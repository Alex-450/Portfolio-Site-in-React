import BlogArchive from '../../Components/BlogArchive';
import { DEFAULT_CATEGORY } from '../../data/blogCategories';

const Page = () => <BlogArchive activeCategory={DEFAULT_CATEGORY} />;

export default Page;
